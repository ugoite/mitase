#[test]
fn behavior_stays_valid() {
    assert!(workbench_flow_fixture::behavior());
}

#[test]
fn added_symbol_lifecycle_stays_valid() {
    assert!(workbench_flow_fixture::behavior());
}

#[test]
fn added_file_lifecycle_stays_valid() {
    assert!(workbench_flow_fixture::behavior());
}

#[test]
fn removed_symbol_lifecycle_stays_valid() {
    assert!(workbench_flow_fixture::behavior());
}

#[test]
fn removed_file_lifecycle_stays_valid() {
    assert!(workbench_flow_fixture::behavior());
}
