schema: syu/work-request/v1
id: WORK-AUTH-FAILURE
title: Keep login failures generic.
operation: modify
origin:
  kind: requirement-criterion
  criterion: REQ-AUTH-001#criterion.invalid-credentials
constraints: { include_facets: [ui, backend, api], exclude_paths: [], max_slices: 3 }
