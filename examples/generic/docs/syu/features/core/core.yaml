schema: syu/spec/v1
kind: features
namespace: generic
category: Generic example
features:
- id: FEAT-GENERIC-001
  title: Generic bootstrap flow
  summary: Keep the smallest starter workspace compatible with the v1 graph.
  status: implemented
  bindings:
  - id: implementation
    role: implementation
    facet: delivery
    responsibility: Implement the owned behavior for this example workspace.
    targets:
    - id: target-1
      adapter: shell
      path: scripts/bootstrap-generic.sh
      selector:
        kind: marker
        value: "#!/usr/bin/env bash"
      claims:
      - kind: satisfies
        criterion: REQ-GENERIC-001#criterion.owned-delivery
