# generic example

This example matches the default `syu init .` starter shape.

It shows the smallest checked-in workspace that still keeps the four layers
explicit and ready to inspect before you scaffold the same shape in your own
repository.

## Try it

```bash
cd examples/generic
syu validate .
syu list requirement
syu show REQ-001
```
