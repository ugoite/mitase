schema: syu/spec/v1
kind: requirements
namespace: typescript-only
category: TypeScript example
requirements:
- id: REQ-TYPESCRIPTONLY-001
  title: TypeScript starter behavior
  description: The TypeScript sample keeps source and test ownership visible.
  priority: high
  status: implemented
  criteria:
  - id: owned-delivery
    kind: behavior
    statement: The TypeScript sample keeps source and test ownership visible.
    governed_by:
    - POL-TYPESCRIPTONLY-001#rule.traceable-delivery
  bindings:
  - id: verification
    role: verification
    facet: verification
    responsibility: Verify the owned example behavior through checked-in evidence.
    targets:
    - id: target-1
      adapter: typescript
      path: src/app.test.ts
      selector:
        kind: file
    verifies:
    - REQ-TYPESCRIPTONLY-001#criterion.owned-delivery
