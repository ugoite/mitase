schema: mitase/spec/v1
kind: philosophies
namespace: csharp-fallback
category: C# fallback example
philosophies:
- id: PHIL-CSHARPFALLBACK-001
  title: C# fallback example
  summary: Show file-level ownership for a language without symbol-level parsing in
    the active v1 toolchain.
  principles:
  - id: exact-ownership
    statement: Fallback ownership still needs explicit source, verification, and automation
      targets.
    applies_to:
    - code
    - work
  bindings: []
