schema: syu/spec/v1
kind: features
namespace: rust-only
category: Rust example
features:
- id: FEAT-RUSTONLY-001
  title: Rust starter behavior
  summary: Keep a Rust-first example aligned with the v1 work graph.
  status: implemented
  bindings:
  - id: implementation
    role: implementation
    facet: delivery
    responsibility: Implement the owned behavior for this example workspace.
    targets:
    - id: target-1
      adapter: shell
      path: src/trace.rs
      selector:
        kind: marker
        value: "pub fn rust_requirement_test() {}"
      claims:
      - kind: satisfies
        criterion: REQ-RUSTONLY-001#criterion.owned-delivery
