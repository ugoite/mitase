schema: syu/spec/v1
kind: philosophies
namespace: browser-ui
category: Browser UI example
philosophies:
- id: PHIL-BROWSERUI-001
  title: Browser UI example
  summary: Keep a small TypeScript UI example traceable through v1 bindings.
  principles:
  - id: exact-ownership
    statement: UI changes must stay connected to explicit implementation and verification
      targets.
    applies_to:
    - code
    - work
  bindings: []
